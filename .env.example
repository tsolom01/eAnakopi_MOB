# API base URL — backend repo: eAnakopi_APIs
#
# Local (iOS simulator):     http://localhost:3000
# Local (Android emulator):  http://10.0.2.2:3000
# AWS dev/prod: get endpoint from ECS Express service (not the service name URL)
# aws ecs describe-express-gateway-service --service-arn arn:aws:ecs:... --query 'service.activeConfigurations[0].ingressPaths[0].endpoint'
#
# EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
